name: Test
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: gcc -Wall -Wextra -std=c89 -pedantic math2.c main.c -o main
      - run: ./main
